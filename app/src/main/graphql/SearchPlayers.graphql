query SearchPlayers($player: String!) {
    getSearchSuggestions(
        input: {
            input: $player
        }) {
        playerSuggestions {
            id
            name
        }
    }
}
